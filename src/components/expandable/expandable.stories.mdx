import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Expandable } from './expandable';
import {Regular} from './expandable.stories'

<Meta title="Components/Expandable" component={Expandable} />

# Expandable

The Expandable content block is used to show expandable text in blogposts. It uses the HTML [detail disclosure element](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/details).

<Canvas>
  <Story story={Regular}/>
</Canvas>

## Component API

<ArgsTable of={Expandable} />