import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Copy } from './copy-button';
import { CodeBlock } from '../../legacy/markdown/code-block';

<Meta title="Components/UI/Copy" component={Copy} />

# Copy

This is a component to copy the passed `text` to the clipboard once the button is pressed.

There is some visual feedback shown, when the button is pressed. Its duration depends on the `props` (default is 3 seconds)

## Regular

<Canvas withSource="open">
  <Story name="Regular">
    <Copy text="Test to be copied!" />
  </Story>
</Canvas>

<ArgsTable of={Copy} />

## Within a Code

<Canvas>
  <Story name="Code">
    <CodeBlock>
      Hier könnte jetzt Code stehen <br />
      Tatsächlicher Code macht leider Storybook kaputt
    </CodeBlock>
  </Story>
</Canvas>
